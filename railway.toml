[build]
builder = "NIXPACKS"

[deploy]
startCommand = "flask db upgrade && python seed.py && gunicorn -w 2 -b 0.0.0.0:$PORT 'app:create_app()'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
